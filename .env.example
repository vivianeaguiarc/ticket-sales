# Copie este arquivo para .env e ajuste os valores conforme seu ambiente.
# Observação: no estado atual do projeto, a conexão MySQL ainda está
# configurada em src/database.ts. Este arquivo documenta a configuração
# recomendada para evolução futura.

DB_HOST=localhost
DB_PORT=3307
DB_USER=root
DB_PASSWORD=root
DB_NAME=tickets

JWT_SECRET=your_secret_key

PORT=3000
