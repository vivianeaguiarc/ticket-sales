# Copie para .env e preencha os valores.
# Variáveis lidas por src/config/env.ts

NODE_ENV=development
HOST=0.0.0.0
PORT=3000

DB_HOST=
DB_PORT=
DB_USER=
DB_PASSWORD=
DB_NAME=

JWT_SECRET=

# Opcional — URL pública da API (Swagger). No Render, RENDER_EXTERNAL_URL é injetado automaticamente.
# API_BASE_URL=https://seu-servico.onrender.com

# Planejado — ainda não implementado no código
CORS_ORIGIN=
